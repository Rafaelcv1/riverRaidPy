to install dependencies:\
pip install -r requirements.txt


to run the game:\
python -m venv venv\
source venv/bin/activate
note: if you are using windows, change the sprite paths of each class with the full path "C:\..."\


todo:
- more maps modules
- refine modules gen
- combustivel
- pontuacao
